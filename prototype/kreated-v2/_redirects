# ==========================================================================
# KREATED V2 — Netlify redirects
#
# ✅ AUDIT GATE LIFTED 2026-09-01, after the production-hardening pass.
#
#    The removed rule was exactly:
#
#        /free-website-audit/*   /contact/   302
#
#    It existed because the page described a product that did not exist: the
#    visitor submitted, and nothing analysed anything. That is no longer true.
#    The full path now works — submit, fetch up to three scored pages,
#    extract signals, classify deterministically, optionally rewrite the prose,
#    recommend through the same engine /pricing/ uses, and hand off to the
#    builder or to /contact/.
#
#    What had to be true before the line came out, and now is:
#      · shared rate limiting that survives instance recycling, not a Map
#        in process memory (netlify/edge-functions/audit-rate-limit.js plus
#        netlify/functions/lib/rate-limit.js)
#      · a crawl capped at three pages, chosen by score and not by link order
#      · an enforced time budget with real headroom under the function timeout
#      · the audit still complete and honest with no model configured
#      · every SSRF control re-tested after those changes
#
#    ⚠ The route is no longer gated at the edge. /free-website-audit/ becomes
#    publicly reachable on the next deploy. `noindex` still applies, as it does
#    on all 19 routes.
#    🚫 If the audit is ever broken again, put the line back rather than
#    weakening the page's copy to match a product that does not work.
# ==========================================================================

# ==========================================================================
# Everything below is ordinary hygiene, not a gate.
# ==========================================================================
