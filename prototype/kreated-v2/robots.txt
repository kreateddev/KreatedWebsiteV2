# ==========================================================================
# KREATED — robots.txt
#
# LAUNCHED FOR SEARCH 2026-09-01 with eighteen indexable routes. Thirty-four as
# of the final content expansion (2026-09-02): +1 vertical page, +14 resources.
# /thanks/ carries "noindex, follow" and is deliberately absent from the sitemap.
#
# ⚠ CANONICAL HOST IS THE APEX, https://kreated.dev/. www 301s to it,
# path-preserving, and every canonical resolves to a 200 in zero hops. 🚫 Do
# not add www URLs to the sitemap or to any canonical.
#
# ⚠ CRAWLING IS OPEN ON PURPOSE. Page-level meta robots is what controls
# indexing here, and a crawler has to fetch a page to read it. "Disallow: /"
# would hide those directives and could leave URLs indexed from external links
# with no content behind them. 🚫 Never disallow a route as a way of
# deindexing it — use the meta tag, as /thanks/ does.
# ==========================================================================

User-agent: *
Allow: /

# The audit endpoint is POST-only; a GET returns 405. Nothing to crawl.
Disallow: /.netlify/functions/

# Netlify's own submission and internal paths. Not pages.
Disallow: /.netlify/

Sitemap: https://kreated.dev/sitemap.xml
